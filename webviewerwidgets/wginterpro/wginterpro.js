widgetGenerators['interpro'] = {
	'variant': {
		'width': 280, 
		'height': 100, 
		'function': function (div, row, tabName) {
			addInfoLine(div, row, 'Domain', 'interpro__domain', tabName);
		}
	}
}