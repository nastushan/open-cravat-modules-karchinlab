widgetGenerators['abraom'] = {
	'variant': {
		'width': 280, 
		'height': 80, 
		'function': function (div, row, tabName) {
			addBarComponent(div, row, 'AF', 'allele_freq', tabName);
		}
	}
}
